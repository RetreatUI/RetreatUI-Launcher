# RetreatUI Launcher 0.3.8

- Corrects **THE BURNING CRUSADE** spelling in the published launcher.
- Removes CoA/TBC banner artwork from the edition cards and replaces it with compact edition badges.
- Removes selection blur and forces sharp ClearType text rendering.
- Rejects source archives and zero-byte feed entries when calculating **Latest on channel**.
- Compares the public feed with both live GitHub release sources and uses only real release assets.
- Keeps launcher self-update protected by release-asset and SHA-256 validation.
