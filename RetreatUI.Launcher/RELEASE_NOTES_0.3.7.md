# RetreatUI Launcher 0.3.7

- Compares the public addon release feed with the live GitHub Releases API.
- Prevents a valid but stale feed from hiding a newly published RetreatUI update.
- Keeps the public feed as a fallback when the GitHub API is unavailable.
- Adds automatic build validation and version-based launcher releases.
