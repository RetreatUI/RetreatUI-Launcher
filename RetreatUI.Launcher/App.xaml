<Application x:Class="RetreatUI.Launcher.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             ShutdownMode="OnMainWindowClose">
    <Application.Resources>
        <SolidColorBrush x:Key="BackgroundBrush" Color="#070B10"/>
        <SolidColorBrush x:Key="PanelBrush" Color="#E5131922"/>
        <SolidColorBrush x:Key="PanelAltBrush" Color="#E81A2230"/>
        <SolidColorBrush x:Key="PanelDeepBrush" Color="#F20B1016"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#DB9930"/>
        <SolidColorBrush x:Key="AccentHoverBrush" Color="#F5B95C"/>
        <SolidColorBrush x:Key="TitleBrush" Color="#F0D29B"/>
        <SolidColorBrush x:Key="TextBrush" Color="#F2F4F7"/>
        <SolidColorBrush x:Key="MutedTextBrush" Color="#9EADC2"/>
        <SolidColorBrush x:Key="SuccessBrush" Color="#71C95A"/>
        <SolidColorBrush x:Key="DangerBrush" Color="#E87570"/>
        <SolidColorBrush x:Key="BorderBrush" Color="#445264"/>
        <SolidColorBrush x:Key="SoftBorderBrush" Color="#253140"/>

        <LinearGradientBrush x:Key="LauncherBackgroundBrush" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#08111D" Offset="0"/>
            <GradientStop Color="#0A0F17" Offset="0.45"/>
            <GradientStop Color="#0B0B10" Offset="1"/>
        </LinearGradientBrush>

        <RadialGradientBrush x:Key="HeaderGlowBrush" Center="0.20,0.08" GradientOrigin="0.20,0.08" RadiusX="0.72" RadiusY="0.62">
            <GradientStop Color="#253A5470" Offset="0"/>
            <GradientStop Color="#0A101800" Offset="1"/>
        </RadialGradientBrush>

        <LinearGradientBrush x:Key="CardFadeBrush" StartPoint="0,0" EndPoint="1,0">
            <GradientStop Color="#F30D1520" Offset="0"/>
            <GradientStop Color="#D70D1520" Offset="0.48"/>
            <GradientStop Color="#440D1520" Offset="0.82"/>
            <GradientStop Color="#090D1520" Offset="1"/>
        </LinearGradientBrush>

        <LinearGradientBrush x:Key="CoAHeroGlowBrush" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#00FFFFFF" Offset="0"/>
            <GradientStop Color="#223A79FF" Offset="0.45"/>
            <GradientStop Color="#1E7F5CFF" Offset="1"/>
        </LinearGradientBrush>

        <LinearGradientBrush x:Key="TBCHeroGlowBrush" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#00FFFFFF" Offset="0"/>
            <GradientStop Color="#20D7922C" Offset="0.42"/>
            <GradientStop Color="#1D40CC7A" Offset="1"/>
        </LinearGradientBrush>

        <LinearGradientBrush x:Key="CoADetailOverlayBrush" StartPoint="0,0" EndPoint="1,0">
            <GradientStop Color="#F5141B26" Offset="0"/>
            <GradientStop Color="#EE121A24" Offset="0.52"/>
            <GradientStop Color="#B4152130" Offset="1"/>
        </LinearGradientBrush>

        <LinearGradientBrush x:Key="TbcDetailOverlayBrush" StartPoint="0,0" EndPoint="1,0">
            <GradientStop Color="#F517181F" Offset="0"/>
            <GradientStop Color="#EB1A1719" Offset="0.52"/>
            <GradientStop Color="#AF201E17" Offset="1"/>
        </LinearGradientBrush>

        <Style TargetType="Window">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Background" Value="{StaticResource BackgroundBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="TextOptions.TextFormattingMode" Value="Display"/>
            <Setter Property="TextOptions.TextRenderingMode" Value="ClearType"/>
        </Style>

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="#FFF8EA"/>
            <Setter Property="Background" Value="#AF6A2617"/>
            <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="20,11"/>
            <Setter Property="MinHeight" Value="42"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="7"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#CC7A3420"/>
                                <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="{StaticResource AccentHoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.82"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.46"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="Background" Value="#CC141C26"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="14,9"/>
            <Setter Property="MinHeight" Value="38"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#E2232D3A"/>
                                <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="{StaticResource AccentHoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.82"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.4"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ChannelRadioStyle" TargetType="RadioButton">
            <Setter Property="Foreground" Value="{StaticResource MutedTextBrush}"/>
            <Setter Property="Background" Value="#8A0E141B"/>
            <Setter Property="Padding" Value="18,9"/>
            <Setter Property="MinWidth" Value="72"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="RadioBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{StaticResource BorderBrush}"
                                BorderThickness="1"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="RadioBorder" Property="Background" Value="#C2362212"/>
                                <Setter TargetName="RadioBorder" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                                <Setter Property="Foreground" Value="#FFF2D1"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="RadioBorder" Property="BorderBrush" Value="{StaticResource AccentHoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="RadioBorder" Property="Opacity" Value="0.42"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Application.Resources>
</Application>
